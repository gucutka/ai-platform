# Architecture Decision Records

ADRs are created by the architecture conversation agent (`write_adr`) or via `draft-adr` CLI.

Use the platform template: `knowledge/technical/templates/adr-template.md`
